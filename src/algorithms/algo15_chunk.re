/* let splitter list i => {
     let rec spl acc i => {
       if (i == 0) {

       }
     }
   }

   let chunk list chunkSize => {
     let rec chunker list acc => {
       if ((List.length list) <= chunkSize) {
         [list, ...acc];
       } else {
         let [chunk, rest] = List.partition(fun  list);
         chunker List.
       }
     }
   }; */
