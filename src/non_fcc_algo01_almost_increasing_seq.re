/**
 * Function takes an array of ints.
 * Returns true if the passed array is increasing.
 * Doesn't have to be sequential, just has to be increasing.
 * Can remove *one* element if need be to make the array sequential.
 * If I have to remove more or it is not sequential from the get-go,
 * then the function returns false.
 */
let rec almostIncreasingSequence arr => {};
